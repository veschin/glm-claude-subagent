{
  "description": "Session with unknown flags that should be passed through to the claude binary unchanged.",
  "args": ["session", "--verbose", "--resume", "abc123"],
  "expected_passthrough": ["--verbose", "--resume", "abc123"],
  "expected_env": {
    "ANTHROPIC_AUTH_TOKEN": "sk-zai-key",
    "ANTHROPIC_BASE_URL": "https://api.z.ai/api/anthropic",
    "ANTHROPIC_DEFAULT_OPUS_MODEL": "glm-4.7",
    "ANTHROPIC_DEFAULT_SONNET_MODEL": "glm-4.7",
    "ANTHROPIC_DEFAULT_HAIKU_MODEL": "glm-4.7"
  },
  "note": "Session mode uses parse_flags 'session' which routes unknown flags to GLM_PASSTHROUGH_ARGS. These are then passed directly to the claude binary invocation. This allows users to use any claude CLI flags (--verbose, --resume, --allowedTools, etc.) transparently."
}
