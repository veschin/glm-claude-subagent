{
  "description": "Session with explicit model override via -m flag. All three model slots should be set to the specified model.",
  "args": ["session", "-m", "glm-4.5"],
  "expected_env": {
    "ANTHROPIC_AUTH_TOKEN": "sk-zai-key",
    "ANTHROPIC_BASE_URL": "https://api.z.ai/api/anthropic",
    "ANTHROPIC_DEFAULT_OPUS_MODEL": "glm-4.5",
    "ANTHROPIC_DEFAULT_SONNET_MODEL": "glm-4.5",
    "ANTHROPIC_DEFAULT_HAIKU_MODEL": "glm-4.5"
  },
  "note": "The -m flag sets the base model which overrides all three slots (opus, sonnet, haiku). Config priority: CLI flags > per-slot env vars > base GLM_MODEL > hardcoded glm-4.7."
}
