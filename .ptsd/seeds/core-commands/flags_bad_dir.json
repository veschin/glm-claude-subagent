{
  "description": "Non-existent directory should return a validation error",
  "args": [
    "run",
    "-d", "/nonexistent/path",
    "Do something"
  ],
  "expected_error": "err:user Directory not found: /nonexistent/path",
  "expected_exit": 1
}
