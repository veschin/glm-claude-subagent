{
  "description": "Unsafe flag sets permission_mode to bypassPermissions",
  "args": [
    "run",
    "--unsafe",
    "Deploy to staging"
  ],
  "expected": {
    "permission_mode": "bypassPermissions",
    "prompt": "Deploy to staging"
  }
}
